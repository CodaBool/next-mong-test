stage: ${env.STAGE}
staging-your-site-name:
  component: '@sls-next/serverless-component@1.18.0'
  inputs:
    memory: 128
    bucketRegion: us-east-1
    bucketName: codabool-stage-test-1
    name: 
      defaultLambda: 
        - "${stage}-"
        - defaut
      apiLambda:
        - "${stage}-"
        - api
    # domain: ['staging-your-site-name', 'a-domain']
<<<<<<< HEAD
    domain:
      - "${stage}-test"
      - codadash.com
    publicDirectoryCache: false
    build:
      env:
        MONGODB_URI: ${env.MONGODB_URI}
        TEST_ENV: ${env.TEST_ENV}
        STAGE: ${env.STAGE}
=======
    publicDirectoryCache: false
    useDotenv: true
    build:
      env:
        MONGODB_URI: ${env.MONGODB_URI}
        TEST_ENV: ${env.TEST_ENV}
>>>>>>> b5b50d689aaf255a47ab7949f9c5411b7ae17d58
